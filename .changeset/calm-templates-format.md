---
"elfui-language-features": patch
---

Honor Prettier single-attribute formatting inside ElfUI templates, suppress the reserved component
`key` prop false positive, and highlight multiline generic and explicitly named bare templates.
