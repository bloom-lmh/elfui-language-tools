---
"elfui-language-features": patch
---

Highlight quoted `v-for` expressions as TypeScript, expose declaration semantic tokens for loop
locals, and infer array loop indexes as `number` for completion and hover.
